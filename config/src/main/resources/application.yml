
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/qq153655101/config-center/
          search-paths: experiments
          username:
          password:
      label: master

server:
  port: 9995  #本地集群两台9994、9995

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:9991/eureka/





